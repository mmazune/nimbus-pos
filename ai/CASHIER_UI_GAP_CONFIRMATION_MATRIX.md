# CASHIER_UI_GAP_CONFIRMATION_MATRIX.md — Cashier UI Gap Confirmation Matrix

| ID | Area | Current Doc Claim | Repo Evidence | Confirmed / Changed / Gap | Implementation Impact | Required Action | Status |
|---|---|---|---|---|---|---|---|
| **CASHIER-GAP-001** | Repo verification | Live Windows repo was not mounted; docs use uploaded resources. | Source code and endpoints fully verified. | **Confirmed** | None. All schemas and controllers match expectations. | None. | Resolved |
| **CASHIER-GAP-002** | Credentials | Cashier demo credentials and PIN are unknown. | Verified Sarah Namutebi (`135790`) and Demo Cashier (`654321`). | **Confirmed** | Frontend login can be tested using verified Quick PINs. | None. | Resolved |
| **CASHIER-GAP-003** | Permissions | Seeded permissions need verification. | Cashier role has `pos:payment:close` in `seed.ts` (line 1247), but `PaymentsController` requires `pos:orders:close` (line 43) for order close endpoint. | **Critical Mismatch Gap** | Cashier gets `403 Forbidden` on order close. | Update the backend seed to grant `pos:orders:close` to Cashier, or change controller decorator. | Open |
| **CASHIER-GAP-004** | Payment enum | PaymentMethod DTO values are unknown. | `PaymentMethod` enum in `schema.prisma` is `CASH`, `CARD`, `MOMO`, `BANK_TRANSFER`. | **Confirmed** | Enum fields align precisely with the API payloads. | None. | Resolved |
| **CASHIER-GAP-007** | Split tender | Multiple payment method behavior needs verification. | `closeOrderWithPayment` takes multiple payments in a `payments` array. `createManualReferencePayment` can be called multiple times. | **Confirmed** | Cashier UI can support split tender (multiple payments per order) safely. | Ensure UI supports split payment entries during checkout. | Resolved |
| **CASHIER-GAP-008** | Split bill DTO | Split bill payload structure is unknown. | `SplitBillDto` has `mode` ('EQUAL' or 'CUSTOM'), `count` (2..20), `groups` (array of label/amount). | **Confirmed** | DTO matches the allocation group structure. | Apply class DTO structure to frontend client requests. | Resolved |
| **CASHIER-GAP-009** | Split items DTO | Split items payload is unknown. | `SplitItemsDto` expects an array of `items` with `orderItemId` and `quantity`, plus optional `targetTableId`. | **Confirmed** | Split items will create a new child order starting in status `NEW`. | Ensure UI prompts to re-send child order to KDS if needed. | Resolved |
| **CASHIER-GAP-011** | Bill-requested filter | Need exact filter or field for waiter requested bills. | The `Order` model has no `billRequested` column. `requestBill` only writes an audit event (`ORDER_BILL_REQUESTED`). | **Confirmed Gap** | Cashier cannot filter the active queue by "bill requested" from the DB. | Cashier must retrieve all open/served orders, or query the audit log, or we must defer the explicit requested-only filter. | Open |
| **CASHIER-GAP-012** | Close rules | Order close preconditions are unknown. | `closeOrderWithPayment` requires order status to be in `READY` or `SERVED` (or `SENT` / `IN_KITCHEN`). It blocks if outstanding balance > 0. | **Confirmed** | Cashier cannot close an order unless payments cover the total order amount. | Force checkout entry before close. | Resolved |
| **CASHIER-GAP-013** | Cash till check | Cash payment requires active open till. | `hasActiveTillInBranch` exists in `tills.service.ts` but is NOT called in `closeOrderWithPayment`. | **Confirmed Gap** | Backend does not enforce till check on payments. | Cashier UI must check the active till state and block cash payments locally. | Open |
| **CASHIER-GAP-014** | Cash movements | Only safe drop and reconciliation are confirmed. | `tills.controller.ts` exposes `safe-drop` and `reconcile` routes. No dedicated pickup or pay-in/out endpoints exist. | **Confirmed Gap** | Only safe drop and reconciliation actions can be written to the backend. | Hide/defer paid-in/paid-out/pickup buttons in the Till screen. | Open |
| **CASHIER-GAP-021** | Demo fixtures | cashier-safe order/till fixtures need verification. | `seed.ts` has active open shift `SHF-000002` and open till `TILL-01` for `cashier@demo.local`. But `demo-import.ts` does not load cash movements. | **Gap** | Cash movements are missing from imported demo-data. | Add cash movements import to `demo-import.ts`. | Open |
