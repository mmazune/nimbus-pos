# ── Nimbus POS API — Environment Placeholder ──
# Copy to .env and fill values. Do NOT commit .env.

API_PORT=3001

# Database (wired in M1)
DATABASE_URL=
DIRECT_DATABASE_URL=

# Auth (wired in M2)
JWT_SECRET=
JWT_REFRESH_SECRET=
