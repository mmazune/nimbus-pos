# ── Nimbus POS API — Environment Placeholder ──
# Copy to .env and fill values. Do NOT commit .env.

API_PORT=3001

# Database (M1)
DATABASE_URL=
DIRECT_DATABASE_URL=

# Redis (wired in later milestones)
REDIS_URL=

# Auth (M2)
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
